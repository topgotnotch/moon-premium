app.connect() {
  if (!app) return console.error("Error While Connecting");
  app.on("Started On " + Date.UTC)
}

app.connection() {
  if (app.error) return console.error("There Was An Error During Processing");
}
